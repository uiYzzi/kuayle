name: Build & Push

on:
  release:
    types: [published]
  pull_request:
    branches: [main]
  push:
    branches: [deploy]

env:
  REGISTRY: ghcr.io

permissions:
  contents: write
  packages: write

jobs:
  build:
    name: Build Images
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main for release build
        if: github.event_name == 'release'
        uses: actions/checkout@v7.0.0
        with:
          ref: main
          fetch-depth: 0

      - name: Checkout
        if: github.event_name != 'release'
        uses: actions/checkout@v7.0.0

      - name: Extract version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            version="${{ github.event.release.tag_name }}"
            number="${version#v}"
          elif [ "${{ github.event_name }}" = "push" ]; then
            version="${GITHUB_REF_NAME}.$(git rev-parse --short HEAD)"
            number="0.0.0"
          else
            version="pr"
            number="0.0.0"
          fi

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "number=$number" >> "$GITHUB_OUTPUT"
          echo "image_prefix=ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      - name: Bump UI version
        if: github.event_name == 'release'
        working-directory: UI
        run: npm version "${{ steps.version.outputs.number }}" --no-git-tag-version --allow-same-version

      - name: Update public release manifest
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl --fail --silent --show-error \
            --header "Authorization: Bearer $GH_TOKEN" \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository }}/releases?per_page=100" \
            | jq '[.[] | {
                tag_name,
                html_url,
                body,
                published_at,
                prerelease,
                force_upgrade: false,
                minimum_supported_version: null,
                upgrade_url: .html_url,
                upgrade_message: null
              }]' \
            > UI/static/releases.json

      - name: Commit version bump
        if: github.event_name == 'release'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add UI/package.json UI/package-lock.json UI/static/releases.json

          if git diff --cached --quiet; then
            echo "Version is already ${{ steps.version.outputs.version }}"
          else
            git commit -m "bump(version): ${{ steps.version.outputs.version }}"
            git push origin HEAD:main
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.1.0

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4.2.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # All-in-one image
      - name: Build & push all-in-one
        uses: docker/build-push-action@v7.2.0
        with:
          context: .
          file: ./Dockerfile
          pull: true
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ steps.version.outputs.image_prefix }}/kuayle:latest
            ${{ steps.version.outputs.image_prefix }}/kuayle:${{ steps.version.outputs.version }}
          cache-from: type=gha,scope=all-in-one
          cache-to: type=gha,mode=max,scope=all-in-one

      # API-only image
      - name: Build & push API
        uses: docker/build-push-action@v7.2.0
        with:
          context: ./BE
          file: ./BE/Dockerfile
          pull: true
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ steps.version.outputs.image_prefix }}/kuayle-api:latest
            ${{ steps.version.outputs.image_prefix }}/kuayle-api:${{ steps.version.outputs.version }}
          cache-from: type=gha,scope=api
          cache-to: type=gha,mode=max,scope=api

      # Web-only image
      - name: Build & push Web
        uses: docker/build-push-action@v7.2.0
        with:
          context: ./UI
          file: ./UI/Dockerfile
          pull: true
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ steps.version.outputs.image_prefix }}/kuayle-web:latest
            ${{ steps.version.outputs.image_prefix }}/kuayle-web:${{ steps.version.outputs.version }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web

  dev-machine-images:
    name: Dev Machine Image (${{ matrix.name }})
    runs-on: ubuntu-latest
    # Fork deploy pushes don't need dev machine images; skip to save CI time.
    if: github.event_name != 'push'
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: gateway
            name: Gateway
            context: ./BE
            dockerfile: ./BE/Dockerfile.machine-gateway
            build_args: ''
          - id: manager
            name: Manager
            context: ./BE
            dockerfile: ./BE/Dockerfile.machine-manager
            build_args: ''
          - id: ide
            name: IDE
            context: ./devmachine
            dockerfile: ./devmachine/ide/Dockerfile
            build_args: ''
          - id: browser
            name: Browser
            context: ./devmachine
            dockerfile: ./devmachine/browser/Dockerfile
            build_args: ''
          - id: collector
            name: Collector
            context: ./devmachine
            dockerfile: ./devmachine/collector/Dockerfile
            build_args: ''
          - id: egress
            name: Egress
            context: ./devmachine
            dockerfile: ./devmachine/egress/Dockerfile
            build_args: ''
          - id: agent-claude
            name: Agent Claude
            context: ./devmachine
            dockerfile: ./devmachine/agent/Dockerfile
            build_args: PROVIDER=claude-code
          - id: agent-opencode
            name: Agent OpenCode
            context: ./devmachine
            dockerfile: ./devmachine/agent/Dockerfile
            build_args: PROVIDER=opencode
          - id: agent-codex
            name: Agent Codex
            context: ./devmachine
            dockerfile: ./devmachine/agent/Dockerfile
            build_args: PROVIDER=codex

    steps:
      - uses: actions/checkout@v7.0.0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4.2.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.2.0

      - name: Build amd64 and arm64 image
        uses: docker/build-push-action@v7.3.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          build-args: ${{ matrix.build_args }}
          pull: true
          push: false
          cache-from: type=gha,scope=dev-machine-${{ matrix.id }}
          cache-to: type=gha,mode=min,scope=dev-machine-${{ matrix.id }}
